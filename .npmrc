# Keep local dependency resolution identical to CI: peer dependencies (e.g.
# @iobroker/adapter-core -> @iobroker/js-controller-common-db, @iobroker/types)
# must be installed and recorded in package-lock.json.
legacy-peer-deps=false
